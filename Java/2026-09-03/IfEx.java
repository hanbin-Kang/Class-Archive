public class IfEx {
    public static void main(String[] args) {

        // ============================================================
        // ■ 제어문 (Control Flow)
        // 특정 조건에 따라 코드의 실행을 제어하기 위해 사용
        // ============================================================


        // ------------------------------------------------------------
        // 1. if ~ else
        // ------------------------------------------------------------

        // 점수가 90점 이상인지 확인

        // int score = 90;
        
        // if (score >= 90) {
        //     System.out.println("90점 이상이다");
        //     System.out.println("학점: A");
        // } else {
        //     System.out.println("90점 미만이다");
        //     System.out.println("학점: F");
        // }


        // ------------------------------------------------------------
        // 2. if ~ else if
        // ------------------------------------------------------------

        // score 값에 따라 학점 출력
        // 90점 이상: A
        // 80점 이상: B
        // 70점 이상: C

        // int score = 90;
        
        // if (score >= 90) {
        //     System.out.println("학점 A");
        // } else if (score >= 80) {
        //     System.out.println("학점 B");
        // } else if (score >= 70) {
        //     System.out.println("학점 C");
        // }


        // ============================================================
        // ■ 연산자 (Operator)
        // ============================================================


        // ------------------------------------------------------------
        // 1. 산술 / 증감 / 복합 대입 연산자
        // ------------------------------------------------------------

        // 산술 연산자
        // +, -, *, /, %

        // 증감 연산자
        // ++, --

        // 복합 대입 연산자
        // +=, -=, *=, /=

        // int x = 10;
        //
        // x++;       // x = x + 1
        // x--;       // x = x - 1
        // x += 10;   // x = x + 10
        // x -= 10;   // x = x - 10


        // ------------------------------------------------------------
        // 2. 전위 / 후위 증감 연산자
        // ------------------------------------------------------------

        // ++x : 값을 먼저 증가시킨 후 사용
        // x++ : 값을 먼저 사용한 후 증가

        // System.out.println(++x);
        // System.out.println(x);


        // ------------------------------------------------------------
        // 3. 비교 연산자
        // ------------------------------------------------------------

        // == : 같다
        // != : 다르다
        // >  : 크다
        // <  : 작다
        // >= : 크거나 같다
        // <= : 작거나 같다


        // ------------------------------------------------------------
        // 4. 논리 연산자
        // ------------------------------------------------------------

        // && : AND
        // || : OR
        // !  : NOT

        // &와 |도 boolean에 사용할 수 있지만,
        // 일반적인 조건식에서는 &&와 ||를 사용한다.


        // ------------------------------------------------------------
        // 5. &와 &&의 차이
        // ------------------------------------------------------------

        // & : 앞의 조건이 거짓이어도 뒤의 조건까지 확인
        // && : 앞의 조건이 거짓이면 뒤의 조건을 확인하지 않음
        //
        // 따라서 &&는 불필요한 뒤쪽 연산을 실행하지 않는
        // 단락 평가(Short-circuit evaluation)를 수행한다.

        // int k = 10;
        // int j = 20;

        // if (k == 10 & j != 20) {
        //     System.out.println("둘 다 만족");
        // }


        // ------------------------------------------------------------
        // 6. |와 ||의 차이
        // ------------------------------------------------------------

        // | : 앞의 조건이 참이어도 뒤의 조건까지 확인
        // || : 앞의 조건이 참이면 뒤의 조건을 확인하지 않음
        //
        // 마찬가지로 ||는 단락 평가를 수행한다.

        // if (k == 10 | j != 20) {
        //     System.out.println("둘 중 하나만 만족");
        // }


        // ------------------------------------------------------------
        // 7. &&의 단락 평가 확인
        // ------------------------------------------------------------

        // 앞의 조건이 false이므로
        // 뒤의 (j += 10)은 실행되지 않는다.

        // if (k > 100 && (j += 10) > 20) {
        //     System.out.println("&& 사용");
        // }

        // System.out.println(j);   // 20


        // ------------------------------------------------------------
        // 8. &의 경우
        // ------------------------------------------------------------

        // 앞의 조건이 false여도
        // 뒤의 (j += 10)을 실행한다.

        // if (k > 100 & (j += 10) > 20) {
        //     System.out.println("& 사용");
        // }

        // System.out.println(j);   // 30
    }
}